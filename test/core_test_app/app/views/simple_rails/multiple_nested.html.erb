<%= netzke :tab_panel_one, {
  height: 200,
  class_name: "SimpleTabPanel",
  items: [:endpoints, :extended_endpoints]
} %>

<%= netzke :tab_panel_two, {
  height: 200,
  class_name: "SimpleTabPanel",
  items: [:extended_endpoints, :endpoints]
} %>
